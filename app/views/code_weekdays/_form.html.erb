<%= form_with(model: code_weekday) do |form| %>
  <% if code_weekday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(code_weekday.errors.count, "error") %> prohibited this code_weekday from being saved:</h2>

      <ul>
        <% code_weekday.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.text_field :priority %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :ref %>
    <%= form.text_field :ref %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
